import { SetToZeroPipe } from './set-to-zero.pipe';

describe('SetToZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new SetToZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
